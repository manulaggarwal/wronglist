# forbidden-strings.txt — CI-greppable constraints
# One literal pattern per line. Any match in <scope dirs> = CI FAIL.
# Managed via EVALS.md §2 — do not edit without updating EVALS.
#
# Usage: grep -rn -f evals/forbidden-strings.txt <scope>/
# CI: ci/check-constraints.sh (exit 1 on any match)
#
# Scope note: point this at generated/production content, NOT historical
# docs that contain the failures you mined these from.
#
# --- examples: replace with yours ---
TODO-in-production
PLACEHOLDER TEXT
Lorem ipsum
